{
  "name": "jiazi-game",
  "version": "0.2.0",
  "description": "甲子纪 - 六十甲子回合制策略卡牌游戏",
  "type": "module",
  "scripts": {
    "build": "npm --prefix app run build",
    "dev": "npm --prefix app run dev",
    "preview": "npm --prefix app run preview",
    "test": "vitest",
    "test:simulator": "python ../docs/analysis/test_simulator.py",
    "export:config": "node scripts/export-config.mjs",
    "sync:simulator": "python scripts/sync-simulator.py",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "^5.9.3",
    "vite": "^6.4.3",
    "vitest": "^4.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
