# === IDE and Editor ===
.vs/
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# === Claude Code Local ===
.claude/settings.local.json
CLAUDE.local.md

# === PilotDeck ===
.pilotdeck/plans/

# === Build Output ===
dist/
# 历史构建残留（dist.bak / dist.prev-* 等），均不进 git
app/dist*/
dist.stale-*/
build/

# === Dependencies ===
node_modules/
.npm/

# Lockfiles: pnpm-lock.yaml is the ONLY lockfile (2026-08-04 固化).
# package-lock.json (root or app/) must never be committed or generated.
package-lock.json
app/package-lock.json
**/package-lock.json

# Vercel config removed 2026-08-04 — deployment is via Cloudflare Pages + Vercel
# dashboard settings, no vercel.json in repo.
vercel.json


# === Vite ===
.vite/

# === TypeScript ===
*.tsbuildinfo

# === Temporary and Cache ===
*.tmp
*.temp
*.cache
*.log
__pycache__/
*.pyc

# pytest cache (Python engine retired 2026-08-04, never recreate)
.pytest_cache/
# wrangler local state
.wrangler/
# safe-delete rename leftovers (2026-08-08 audit)
*.deleted-tmp

# === Archives ===
*.zip
*.tar.gz
*.rar
*.7z

# === Secrets and Environment ===
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.json

# === Large Binary Assets (use Git LFS instead) ===
# Uncomment and configure with Git LFS for your project:
# *.png
# *.jpg
# *.jpeg
# *.wav
# *.mp3
# *.ogg
# *.fbx
# *.blend
# *.psd
# *.tga
# *.exr
# *.hdr

# === OS Generated ===
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# === Game Engine & Editor Cache ===
.godot/
.codex/

# Preview files
.preview
.cozeproj/reduction

# Browser test artifacts (never commit reports, traces, screenshots, or results)
.playwright-cli/
playwright-report/
test-results/

.vercel
.git.bak*
.git.corrupt*
.tmp-restore/
